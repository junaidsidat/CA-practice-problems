num = int(input('enter number here: '))
lst = []
for number in range(1, num):
    if num % number == 0:
        lst.append(number)
print(lst)

# prompts the user to enter a number
# prints out new list that shows all the divisors of the number that was specified by the user
